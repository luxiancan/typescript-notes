// 元祖（Tuple），固定长度的数组 Array

export {} // 确保跟其他示例没有成员冲突

const tuple: [number, string] = [18, 'lxcan'];

const age = tuple[0];
const name = tuple[1];

// =============================

// Object.entries({
//     name: 'lxcan',
//     age: 18
// })

// 返回的每个元素也是元祖，固定长度的数组